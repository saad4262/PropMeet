import 'package:flutter/material.dart';

class LoginOptionViews extends StatelessWidget {
  const LoginOptionViews({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
  body: Center(
    child: Text('login option View'),
  ),
    );
  }
}
